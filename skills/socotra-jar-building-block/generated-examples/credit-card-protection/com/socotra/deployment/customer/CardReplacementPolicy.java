/*
  Autogenerated - DO NOT EDIT!
*/
package com.socotra.deployment.customer;

import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonView;
import com.fasterxml.jackson.core.type.*;
import com.socotra.coremodel.*;
import com.socotra.coremodel.views.Internal;
import com.socotra.deployment.*;
import com.socotra.platform.tools.ULID;

import java.math.*;
import java.time.*;
import java.util.*;
import java.util.function.Consumer;
import java.util.regex.*;
import java.util.stream.Collectors;

public record CardReplacementPolicy(
    @JsonView({Internal.class}) Element element
    ) implements CardReplacement, Validatable, Elemental, CustomerObject {

  public static final String TYPE = "CardReplacementPolicy";

  public String type() { return TYPE; }

  public CardReplacementPolicy maskData(DataMaskingLevel level) {
    return this;
  }

  public CardReplacementPolicy anonymizeData() {
    return this;
  }

  public Instant originalEffectiveTime() {
    return element.originalEffectiveTime().orElse(null);
  }
  @Override
  public Collection<ValidationItem> validate(DeploymentConfig config, ValidatableContext context) {
    Collection<ValidationItem> validationItems = new ArrayList<>();
    ValidationItem.ValidationItemBuilder validationItemBuilder = ValidationItem.builder()
        .elementType(type()).locator(element.locator());
    if (validationItemBuilder.hasErrors()) {
      validationItems.add(validationItemBuilder.build());
    }
    return validationItems;
  }

  public CardReplacementPolicy applyDefaults(DeploymentConfig config) {
    CardReplacementPolicyBuilder builder = toBuilder();
    return builder.build();
  }

  public CardReplacementPolicy applyAvailabilityRemovals(Instant referenceDate) {
    CardReplacementPolicyBuilder builder = toBuilder();
    return builder.build();
  }

  @JsonIgnore
  public ULID locator() {
    return element.locator();
  }

  public Element toElement(DeploymentFactory factory) {
    Map<String, Object> ct = Map.of();
    Collection<Element> subElements = List.of();
    Map<String, Object> data = Map.of();
    return element.toBuilder()
        .type(type())
        .elements(Collections.unmodifiableCollection(subElements))
        .coverageTerms(ct)
        .data(data)
        .category(ElementCategory.coverage)
        .build();
  }

  public static CardReplacementPolicy fromElement(DeploymentFactory factory, Element element) {
    if (element == null) {
      return null;
    }
    return new CardReplacementPolicy(
        element);
  }

  public static CardReplacementPolicy copyFrom(UUID tenantLocator, ULID rootLocator, ULID parentLocator, CardReplacementQuote other) {
    if (other == null) {
      return null;
    }
    CardReplacementPolicyBuilder builder = builder(tenantLocator, rootLocator, parentLocator, other.element().locator(), other.element().originalEffectiveTime().orElse(null));
    return builder
        .build();
  }

  public static CardReplacementPolicy copyFrom(UUID tenantLocator, ULID rootLocator, ULID parentLocator, CardReplacementPolicy other) {
    if (other == null) {
      return null;
    }
    CardReplacementPolicyBuilder builder = builder(tenantLocator, rootLocator, parentLocator, other.element().staticLocator(), other.element().originalEffectiveTime().orElse(null));
    return builder
        .build();
  }

  public static CardReplacementPolicyBuilder builder(UUID tenantLocator, ULID rootLocator, ULID parentLocator, ULID staticLocator, Instant originalEffectiveTime) {
    Element origin = Element.builder()
        .tenantLocator(tenantLocator)
        .type("CardReplacementPolicy")
        .locator(ULID.generate())
        .staticLocator(staticLocator)
        .rootLocator(rootLocator)
        .parentLocator(parentLocator)
        .originalEffectiveTime(originalEffectiveTime)
        .build();
    return new CardReplacementPolicyBuilder().element(origin);
  }

  public CardReplacementPolicyBuilder toBuilder() {
    return new CardReplacementPolicyBuilder()
        .element(this.element);
  }

  public static class CardReplacementPolicyBuilder {
    private Element element;
    public CardReplacementPolicyBuilder element(Element element) { this.element = element; return this; }

    private CardReplacementPolicyBuilder() {}

    public CardReplacementPolicy build() {
      return new CardReplacementPolicy(
          this.element
      );
    }
  }

}
