/*
  Autogenerated - DO NOT EDIT!
*/
package com.socotra.deployment.customer;

import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonView;
import com.fasterxml.jackson.core.type.*;
import com.socotra.coremodel.*;
import com.socotra.coremodel.views.Internal;
import com.socotra.deployment.*;
import com.socotra.platform.tools.ULID;

import java.math.*;
import java.time.*;
import java.util.*;
import java.util.function.Consumer;
import java.util.regex.*;
import java.util.stream.Collectors;

public record PurchaseProtectionPolicy(
    @JsonView({Internal.class}) Element element
    ) implements PurchaseProtection, Validatable, Elemental, CustomerObject {

  public static final String TYPE = "PurchaseProtectionPolicy";

  public String type() { return TYPE; }

  public PurchaseProtectionPolicy maskData(DataMaskingLevel level) {
    return this;
  }

  public PurchaseProtectionPolicy anonymizeData() {
    return this;
  }

  public Instant originalEffectiveTime() {
    return element.originalEffectiveTime().orElse(null);
  }
  @Override
  public Collection<ValidationItem> validate(DeploymentConfig config, ValidatableContext context) {
    Collection<ValidationItem> validationItems = new ArrayList<>();
    ValidationItem.ValidationItemBuilder validationItemBuilder = ValidationItem.builder()
        .elementType(type()).locator(element.locator());
    if (validationItemBuilder.hasErrors()) {
      validationItems.add(validationItemBuilder.build());
    }
    return validationItems;
  }

  public PurchaseProtectionPolicy applyDefaults(DeploymentConfig config) {
    PurchaseProtectionPolicyBuilder builder = toBuilder();
    return builder.build();
  }

  public PurchaseProtectionPolicy applyAvailabilityRemovals(Instant referenceDate) {
    PurchaseProtectionPolicyBuilder builder = toBuilder();
    return builder.build();
  }

  @JsonIgnore
  public ULID locator() {
    return element.locator();
  }

  public Element toElement(DeploymentFactory factory) {
    Map<String, Object> ct = Map.of();
    Collection<Element> subElements = List.of();
    Map<String, Object> data = Map.of();
    return element.toBuilder()
        .type(type())
        .elements(Collections.unmodifiableCollection(subElements))
        .coverageTerms(ct)
        .data(data)
        .category(ElementCategory.coverage)
        .build();
  }

  public static PurchaseProtectionPolicy fromElement(DeploymentFactory factory, Element element) {
    if (element == null) {
      return null;
    }
    return new PurchaseProtectionPolicy(
        element);
  }

  public static PurchaseProtectionPolicy copyFrom(UUID tenantLocator, ULID rootLocator, ULID parentLocator, PurchaseProtectionQuote other) {
    if (other == null) {
      return null;
    }
    PurchaseProtectionPolicyBuilder builder = builder(tenantLocator, rootLocator, parentLocator, other.element().locator(), other.element().originalEffectiveTime().orElse(null));
    return builder
        .build();
  }

  public static PurchaseProtectionPolicy copyFrom(UUID tenantLocator, ULID rootLocator, ULID parentLocator, PurchaseProtectionPolicy other) {
    if (other == null) {
      return null;
    }
    PurchaseProtectionPolicyBuilder builder = builder(tenantLocator, rootLocator, parentLocator, other.element().staticLocator(), other.element().originalEffectiveTime().orElse(null));
    return builder
        .build();
  }

  public static PurchaseProtectionPolicyBuilder builder(UUID tenantLocator, ULID rootLocator, ULID parentLocator, ULID staticLocator, Instant originalEffectiveTime) {
    Element origin = Element.builder()
        .tenantLocator(tenantLocator)
        .type("PurchaseProtectionPolicy")
        .locator(ULID.generate())
        .staticLocator(staticLocator)
        .rootLocator(rootLocator)
        .parentLocator(parentLocator)
        .originalEffectiveTime(originalEffectiveTime)
        .build();
    return new PurchaseProtectionPolicyBuilder().element(origin);
  }

  public PurchaseProtectionPolicyBuilder toBuilder() {
    return new PurchaseProtectionPolicyBuilder()
        .element(this.element);
  }

  public static class PurchaseProtectionPolicyBuilder {
    private Element element;
    public PurchaseProtectionPolicyBuilder element(Element element) { this.element = element; return this; }

    private PurchaseProtectionPolicyBuilder() {}

    public PurchaseProtectionPolicy build() {
      return new PurchaseProtectionPolicy(
          this.element
      );
    }
  }

}
