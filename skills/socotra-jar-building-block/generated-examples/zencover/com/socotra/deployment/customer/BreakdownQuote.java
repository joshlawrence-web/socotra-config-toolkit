/*
  Autogenerated - DO NOT EDIT!
*/
package com.socotra.deployment.customer;

import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonView;
import com.fasterxml.jackson.core.type.*;
import com.socotra.coremodel.*;
import com.socotra.coremodel.constraints.*;
import com.socotra.coremodel.views.Internal;
import com.socotra.deployment.*;
import com.socotra.platform.tools.ULID;

import java.math.*;
import java.time.*;
import java.util.*;
import java.util.function.Consumer;
import java.util.regex.*;
import java.util.stream.Collectors;

public record BreakdownQuote(
    ZeroDeductible zeroDeductible,
    BreakdownQuoteData data,
    @JsonView({Internal.class}) Element element
    ) implements Breakdown, Validatable<BreakdownQuote>, Elemental, CustomerObject, SensitiveDataHolder<BreakdownQuote.BreakdownQuoteData> {

  public static final String TYPE = "BreakdownQuote";

  public String type() { return TYPE; }

  public BreakdownQuote maskData(DataMaskingLevel level) {
    BreakdownQuoteBuilder builder = toBuilder();
    builder.data(this.data.maskData(level));
    return builder.build();
  }

  public BreakdownQuote anonymizeData() {
    BreakdownQuoteBuilder builder = toBuilder();
    builder.data(this.data.anonymizeData());
    return builder.build();
  }

  public BreakdownQuote {
    if(data == null) {
      data = BreakdownQuoteData.builder().build();
    }
  }

  @Override
  public Collection<ValidationItem> validate(DeploymentConfig config, ValidatableContext context) {
    Collection<ValidationItem> validationItems = new ArrayList<>();
    ValidationItem.ValidationItemBuilder validationItemBuilder = ValidationItem.builder()
        .elementType(type()).locator(element.locator());
    if (this.zeroDeductible == null) {
      validationItemBuilder.addError("'zeroDeductible' is required");
    } else {
      validationItems.addAll(this.zeroDeductible.validate(config, context));
    }

    this.data.validate(config, context).forEach(error -> error.errors().forEach(validationItemBuilder::addError));
    if (validationItemBuilder.hasErrors()) {
      validationItems.add(validationItemBuilder.build());
    }
    return validationItems;
  }

  @Override
  public BreakdownQuote correct(DeploymentConfig config, ValidationErrorResolver resolver) {
    BreakdownQuoteBuilder builder = toBuilder();
    Map<String, java.lang.reflect.RecordComponent> fields = Arrays.stream(this.getClass().getRecordComponents()).collect(Collectors.toMap(java.lang.reflect.RecordComponent::getName, c -> c));
    if(zeroDeductible != null) {
      builder.zeroDeductible(zeroDeductible.correct(config, resolver));
    } else {
      resolver.correct(this, fields.get("zeroDeductible"), builder::zeroDeductible, new Required<>(zeroDeductible));
    }
    builder.data(this.data.correct(config, resolver));

    return builder.build();
  }

  public BreakdownQuote applyDefaults(DeploymentConfig config) {
    BreakdownQuoteBuilder builder = toBuilder();
    if (this.zeroDeductible == null) { builder.zeroDeductible(com.socotra.deployment.customer.ZeroDeductible.defaultOption()); }
    return builder.build();
  }

  public BreakdownQuote applyAvailabilityRemovals(Instant referenceDate) {
    BreakdownQuoteBuilder builder = toBuilder();
    if (this.zeroDeductible != null) {
      builder.zeroDeductible(zeroDeductible.applyAvailabilityRemovals(referenceDate));
    }

    builder.data(this.data.applyAvailabilityRemovals(referenceDate));
    return builder.build();
  }

  @JsonIgnore
  public ULID locator() {
    return element.locator();
  }

  public Element toElement(DeploymentFactory factory) {
    Map<String, Object> ct = new HashMap<>();
    if (this.zeroDeductible != null) {
      ct.put("ZeroDeductible", this.zeroDeductible);
    }
    Collection<Element> subElements = List.of();
    Map<String, Object> data = CustomerDataHolder.transform(factory, this.data());
    return element.toBuilder()
        .type(type())
        .elements(Collections.unmodifiableCollection(subElements))
        .coverageTerms(ct)
        .data(data)
        .category(ElementCategory.coverage)
        .build();
  }

  public static BreakdownQuote fromElement(DeploymentFactory factory, Element element) {
    if (element == null) {
      return null;
    }
    return new BreakdownQuote(
        com.socotra.deployment.customer.ZeroDeductible.fromMap(element.coverageTerms()),
        element.resolve(BreakdownQuoteData.TYPE, factory),
        element);
  }

  public static BreakdownQuoteBuilder builder(UUID tenantLocator, ULID rootLocator, ULID parentLocator, ULID staticLocator, Instant originalEffectiveTime) {
    Element origin = Element.builder()
        .tenantLocator(tenantLocator)
        .type("BreakdownQuote")
        .locator(ULID.generate())
        .staticLocator(staticLocator)
        .rootLocator(rootLocator)
        .parentLocator(parentLocator)
        .originalEffectiveTime(originalEffectiveTime)
        .build();
    return new BreakdownQuoteBuilder().element(origin).data(BreakdownQuoteData.builder().build());
  }

  public BreakdownQuoteBuilder toBuilder() {
    return new BreakdownQuoteBuilder()
        .zeroDeductible(this.zeroDeductible)

        .data(this.data)
        .element(this.element);
  }

  public static class BreakdownQuoteBuilder {
    private ZeroDeductible zeroDeductible;
    public BreakdownQuoteBuilder zeroDeductible(ZeroDeductible zeroDeductible) { this.zeroDeductible = zeroDeductible; return this; }

    private BreakdownQuoteData data;
    public BreakdownQuoteBuilder data(BreakdownQuoteData data) { this.data = data; return this; }
    private Element element;
    public BreakdownQuoteBuilder element(Element element) { this.element = element; return this; }

    private BreakdownQuoteBuilder() {}

    public BreakdownQuote build() {
      return new BreakdownQuote(
          this.zeroDeductible,
          this.data,
          this.element
      );
    }
  }

  public record BreakdownQuoteData(String labourCovered, LocalDate manufacturerLabourGuaranteeEndDate, LocalDate manufacturerPartsGuaranteeEndDate, String partsCovered) implements BreakdownData, CustomerObject, Validatable<BreakdownQuoteData> {
    public static final String TYPE = "BreakdownQuoteData";
    public static final Set<String> labourCoveredOptions = Set.of("true","false");
    public static final Set<String> partsCoveredOptions = Set.of("true","false");

    public BreakdownQuoteData {
    if (labourCovered == null || labourCovered.isBlank()) { labourCovered = "true";}
    if (partsCovered == null || partsCovered.isBlank()) { partsCovered = "true";}
    }

    public String type() { return TYPE; }

    public BreakdownQuoteData maskData(DataMaskingLevel level) {
      BreakdownQuoteDataBuilder builder = toBuilder();
      if (level == DataMaskingLevel.level1) {
      }
      if (level == DataMaskingLevel.level2) {
      }
      if (level == DataMaskingLevel.none) {
      }
      return builder.build();
    }

    public BreakdownQuoteData anonymizeData() {
      BreakdownQuoteDataBuilder builder = toBuilder();
      return builder.build();
    }

    @Override
    public Collection<ValidationItem> validate(DeploymentConfig config, ValidatableContext context) {
      ValidationItem.ValidationItemBuilder validationItemBuilder = ValidationItem.builder().elementType(TYPE);
      if (labourCovered != null && labourCovered.length() > 20000) { validationItemBuilder.addError("'breakdownQuoteData.labourCovered' length is more than max length of 20000"); }
      if (labourCovered != null && !labourCoveredOptions.contains(labourCovered)) { validationItemBuilder.addError("Property 'breakdownQuoteData.labourCovered' should be one of " + labourCoveredOptions); }
      if (partsCovered != null && partsCovered.length() > 20000) { validationItemBuilder.addError("'breakdownQuoteData.partsCovered' length is more than max length of 20000"); }
      if (partsCovered != null && !partsCoveredOptions.contains(partsCovered)) { validationItemBuilder.addError("Property 'breakdownQuoteData.partsCovered' should be one of " + partsCoveredOptions); }

      return validationItemBuilder.hasErrors() ? List.of(validationItemBuilder.build()) : List.of();
    }

    @Override
    public BreakdownQuoteData correct(DeploymentConfig config, ValidationErrorResolver resolver) {
      BreakdownQuoteDataBuilder builder = this.toBuilder();
      Map<String, java.lang.reflect.RecordComponent> fields = Arrays.stream(this.getClass().getRecordComponents()).collect(java.util.stream.Collectors.toMap(java.lang.reflect.RecordComponent::getName, c -> c));
      resolver.correct(this, fields.get("labourCovered"), builder::labourCovered, new MaxLength(20000), new Options<>(labourCoveredOptions));
      resolver.correct(this, fields.get("partsCovered"), builder::partsCovered, new MaxLength(20000), new Options<>(partsCoveredOptions));

      return builder.build();
    }

    @Override
    public BreakdownQuoteData applyAvailabilityRemovals(Instant referenceDate) {
      BreakdownQuoteDataBuilder builder = toBuilder();

      return builder.build();
    }

    public static BreakdownQuoteDataBuilder builder() {
      return new BreakdownQuoteDataBuilder();
    }
    public BreakdownQuoteDataBuilder toBuilder() {
      return new BreakdownQuoteDataBuilder()
      .labourCovered(this.labourCovered)
      .manufacturerLabourGuaranteeEndDate(this.manufacturerLabourGuaranteeEndDate)
      .manufacturerPartsGuaranteeEndDate(this.manufacturerPartsGuaranteeEndDate)
      .partsCovered(this.partsCovered);
    }

    public static class BreakdownQuoteDataBuilder {
      private String labourCovered;
      public BreakdownQuoteDataBuilder labourCovered(String labourCovered) { this.labourCovered = labourCovered; return this; }
      private LocalDate manufacturerLabourGuaranteeEndDate;
      public BreakdownQuoteDataBuilder manufacturerLabourGuaranteeEndDate(LocalDate manufacturerLabourGuaranteeEndDate) { this.manufacturerLabourGuaranteeEndDate = manufacturerLabourGuaranteeEndDate; return this; }
      private LocalDate manufacturerPartsGuaranteeEndDate;
      public BreakdownQuoteDataBuilder manufacturerPartsGuaranteeEndDate(LocalDate manufacturerPartsGuaranteeEndDate) { this.manufacturerPartsGuaranteeEndDate = manufacturerPartsGuaranteeEndDate; return this; }
      private String partsCovered;
      public BreakdownQuoteDataBuilder partsCovered(String partsCovered) { this.partsCovered = partsCovered; return this; }

      public BreakdownQuoteData build() {
        return new BreakdownQuoteData(labourCovered, manufacturerLabourGuaranteeEndDate, manufacturerPartsGuaranteeEndDate, partsCovered);
      }
    }
  }

  public static boolean canAccessFields(DataMaskingLevel level, Map<String, Object> fields) {
    return true;
  }
  public static DataMaskingLevel fieldsMaximumLevel(Map<String, Object> fields) {
    DataMaskingLevel maxLevel = DataMaskingLevel.none;
    return maxLevel;
  }
  public static Map<String, Object> anonymizeFields(Map<String, Object> fields) {
    Map<String, Object> result = new HashMap<>();
    for (Map.Entry<String, Object> entry: fields.entrySet()) {
      String keyName = entry.getKey();
      Object fieldValue = entry.getValue();
      result.put(keyName, fieldValue);
    }
    return result;
  }
}