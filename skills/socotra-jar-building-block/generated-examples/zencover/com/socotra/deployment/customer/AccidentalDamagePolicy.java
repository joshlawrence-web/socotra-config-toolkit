/*
  Autogenerated - DO NOT EDIT!
*/
package com.socotra.deployment.customer;

import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonView;
import com.fasterxml.jackson.core.type.*;
import com.socotra.coremodel.*;
import com.socotra.coremodel.constraints.*;
import com.socotra.coremodel.views.Internal;
import com.socotra.deployment.*;
import com.socotra.platform.tools.ULID;

import java.math.*;
import java.time.*;
import java.util.*;
import java.util.function.Consumer;
import java.util.regex.*;
import java.util.stream.Collectors;

public record AccidentalDamagePolicy(
    ZeroDeductible zeroDeductible,
    AccidentalDamagePolicyData data,
    @JsonView({Internal.class}) Element element
    ) implements AccidentalDamage, Validatable<AccidentalDamagePolicy>, Elemental, CustomerObject, SensitiveDataHolder<AccidentalDamagePolicy.AccidentalDamagePolicyData> {

  public static final String TYPE = "AccidentalDamagePolicy";

  public String type() { return TYPE; }

  public AccidentalDamagePolicy maskData(DataMaskingLevel level) {
    AccidentalDamagePolicyBuilder builder = toBuilder();
    builder.data(this.data.maskData(level));
    return builder.build();
  }

  public AccidentalDamagePolicy anonymizeData() {
    AccidentalDamagePolicyBuilder builder = toBuilder();
    builder.data(this.data.anonymizeData());
    return builder.build();
  }

  public AccidentalDamagePolicy {
    if(data == null) {
      data = AccidentalDamagePolicyData.builder().build();
    }
  }

  public Instant originalEffectiveTime() {
    return element.originalEffectiveTime().orElse(null);
  }
  @Override
  public Collection<ValidationItem> validate(DeploymentConfig config, ValidatableContext context) {
    Collection<ValidationItem> validationItems = new ArrayList<>();
    ValidationItem.ValidationItemBuilder validationItemBuilder = ValidationItem.builder()
        .elementType(type()).locator(element.locator());
    if (this.zeroDeductible == null) {
      validationItemBuilder.addError("'zeroDeductible' is required");
    } else {
      validationItems.addAll(this.zeroDeductible.validate(config, context));
    }

    this.data.validate(config, context).forEach(error -> error.errors().forEach(validationItemBuilder::addError));
    if (validationItemBuilder.hasErrors()) {
      validationItems.add(validationItemBuilder.build());
    }
    return validationItems;
  }

  @Override
  public AccidentalDamagePolicy correct(DeploymentConfig config, ValidationErrorResolver resolver) {
    AccidentalDamagePolicyBuilder builder = toBuilder();
    Map<String, java.lang.reflect.RecordComponent> fields = Arrays.stream(this.getClass().getRecordComponents()).collect(Collectors.toMap(java.lang.reflect.RecordComponent::getName, c -> c));
    if(zeroDeductible != null) {
      builder.zeroDeductible(zeroDeductible.correct(config, resolver));
    } else {
      resolver.correct(this, fields.get("zeroDeductible"), builder::zeroDeductible, new Required<>(zeroDeductible));
    }
    builder.data(this.data.correct(config, resolver));

    return builder.build();
  }

  public AccidentalDamagePolicy applyDefaults(DeploymentConfig config) {
    AccidentalDamagePolicyBuilder builder = toBuilder();
    if (this.zeroDeductible == null) { builder.zeroDeductible(com.socotra.deployment.customer.ZeroDeductible.defaultOption()); }
    return builder.build();
  }

  public AccidentalDamagePolicy applyAvailabilityRemovals(Instant referenceDate) {
    AccidentalDamagePolicyBuilder builder = toBuilder();
    if (this.zeroDeductible != null) {
      builder.zeroDeductible(zeroDeductible.applyAvailabilityRemovals(referenceDate));
    }

    builder.data(this.data.applyAvailabilityRemovals(referenceDate));
    return builder.build();
  }

  @JsonIgnore
  public ULID locator() {
    return element.locator();
  }

  public Element toElement(DeploymentFactory factory) {
    Map<String, Object> ct = new HashMap<>();
    if (this.zeroDeductible != null) {
      ct.put("ZeroDeductible", this.zeroDeductible);
    }
    Collection<Element> subElements = List.of();
    Map<String, Object> data = CustomerDataHolder.transform(factory, this.data());
    return element.toBuilder()
        .type(type())
        .elements(Collections.unmodifiableCollection(subElements))
        .coverageTerms(ct)
        .data(data)
        .category(ElementCategory.coverage)
        .build();
  }

  public static AccidentalDamagePolicy fromElement(DeploymentFactory factory, Element element) {
    if (element == null) {
      return null;
    }
    return new AccidentalDamagePolicy(
        com.socotra.deployment.customer.ZeroDeductible.fromMap(element.coverageTerms()),
        element.resolve(AccidentalDamagePolicyData.TYPE, factory),
        element);
  }

  public static AccidentalDamagePolicy copyFrom(UUID tenantLocator, ULID rootLocator, ULID parentLocator, AccidentalDamageQuote other) {
    if (other == null) {
      return null;
    }
    AccidentalDamagePolicyBuilder builder = builder(tenantLocator, rootLocator, parentLocator, other.element().locator(), other.element().originalEffectiveTime().orElse(null));
    return builder
        .zeroDeductible(other.zeroDeductible())
        .data(AccidentalDamagePolicyData.copyFrom(other.data()))
        .build();
  }

  public static AccidentalDamagePolicy copyFrom(UUID tenantLocator, ULID rootLocator, ULID parentLocator, AccidentalDamagePolicy other) {
    if (other == null) {
      return null;
    }
    AccidentalDamagePolicyBuilder builder = builder(tenantLocator, rootLocator, parentLocator, other.element().staticLocator(), other.element().originalEffectiveTime().orElse(null));
    return builder
        .zeroDeductible(other.zeroDeductible())
        .data(AccidentalDamagePolicyData.copyFrom(other.data()))
        .build();
  }

  public static AccidentalDamagePolicyBuilder builder(UUID tenantLocator, ULID rootLocator, ULID parentLocator, ULID staticLocator, Instant originalEffectiveTime) {
    Element origin = Element.builder()
        .tenantLocator(tenantLocator)
        .type("AccidentalDamagePolicy")
        .locator(ULID.generate())
        .staticLocator(staticLocator)
        .rootLocator(rootLocator)
        .parentLocator(parentLocator)
        .originalEffectiveTime(originalEffectiveTime)
        .build();
    return new AccidentalDamagePolicyBuilder().element(origin).data(AccidentalDamagePolicyData.builder().build());
  }

  public AccidentalDamagePolicyBuilder toBuilder() {
    return new AccidentalDamagePolicyBuilder()
        .zeroDeductible(this.zeroDeductible)

        .data(this.data)
        .element(this.element);
  }

  public static class AccidentalDamagePolicyBuilder {
    private ZeroDeductible zeroDeductible;
    public AccidentalDamagePolicyBuilder zeroDeductible(ZeroDeductible zeroDeductible) { this.zeroDeductible = zeroDeductible; return this; }

    private AccidentalDamagePolicyData data;
    public AccidentalDamagePolicyBuilder data(AccidentalDamagePolicyData data) { this.data = data; return this; }
    private Element element;
    public AccidentalDamagePolicyBuilder element(Element element) { this.element = element; return this; }

    private AccidentalDamagePolicyBuilder() {}

    public AccidentalDamagePolicy build() {
      return new AccidentalDamagePolicy(
          this.zeroDeductible,
          this.data,
          this.element
      );
    }
  }

  public record AccidentalDamagePolicyData(String labourCovered, String partsCovered) implements AccidentalDamageData, CustomerObject, Validatable<AccidentalDamagePolicyData> {
    public static final String TYPE = "AccidentalDamagePolicyData";
    public static final Set<String> labourCoveredOptions = Set.of("true","false");
    public static final Set<String> partsCoveredOptions = Set.of("true","false");

    public AccidentalDamagePolicyData {
    if (labourCovered == null || labourCovered.isBlank()) { labourCovered = "true";}
    if (partsCovered == null || partsCovered.isBlank()) { partsCovered = "true";}
    }

    public String type() { return TYPE; }

    public AccidentalDamagePolicyData maskData(DataMaskingLevel level) {
      AccidentalDamagePolicyDataBuilder builder = toBuilder();
      if (level == DataMaskingLevel.level1) {
      }
      if (level == DataMaskingLevel.level2) {
      }
      if (level == DataMaskingLevel.none) {
      }
      return builder.build();
    }

    public AccidentalDamagePolicyData anonymizeData() {
      AccidentalDamagePolicyDataBuilder builder = toBuilder();
      return builder.build();
    }

    @Override
    public Collection<ValidationItem> validate(DeploymentConfig config, ValidatableContext context) {
      ValidationItem.ValidationItemBuilder validationItemBuilder = ValidationItem.builder().elementType(TYPE);
      if (labourCovered != null && labourCovered.length() > 20000) { validationItemBuilder.addError("'accidentalDamagePolicyData.labourCovered' length is more than max length of 20000"); }
      if (labourCovered != null && !labourCoveredOptions.contains(labourCovered)) { validationItemBuilder.addError("Property 'accidentalDamagePolicyData.labourCovered' should be one of " + labourCoveredOptions); }
      if (partsCovered != null && partsCovered.length() > 20000) { validationItemBuilder.addError("'accidentalDamagePolicyData.partsCovered' length is more than max length of 20000"); }
      if (partsCovered != null && !partsCoveredOptions.contains(partsCovered)) { validationItemBuilder.addError("Property 'accidentalDamagePolicyData.partsCovered' should be one of " + partsCoveredOptions); }

      return validationItemBuilder.hasErrors() ? List.of(validationItemBuilder.build()) : List.of();
    }

    @Override
    public AccidentalDamagePolicyData correct(DeploymentConfig config, ValidationErrorResolver resolver) {
      AccidentalDamagePolicyDataBuilder builder = this.toBuilder();
      Map<String, java.lang.reflect.RecordComponent> fields = Arrays.stream(this.getClass().getRecordComponents()).collect(java.util.stream.Collectors.toMap(java.lang.reflect.RecordComponent::getName, c -> c));
      resolver.correct(this, fields.get("labourCovered"), builder::labourCovered, new MaxLength(20000), new Options<>(labourCoveredOptions));
      resolver.correct(this, fields.get("partsCovered"), builder::partsCovered, new MaxLength(20000), new Options<>(partsCoveredOptions));

      return builder.build();
    }

    @Override
    public AccidentalDamagePolicyData applyAvailabilityRemovals(Instant referenceDate) {
      AccidentalDamagePolicyDataBuilder builder = toBuilder();

      return builder.build();
    }

    public static AccidentalDamagePolicyDataBuilder builder() {
      return new AccidentalDamagePolicyDataBuilder();
    }
    public AccidentalDamagePolicyDataBuilder toBuilder() {
      return new AccidentalDamagePolicyDataBuilder()
      .labourCovered(this.labourCovered)
      .partsCovered(this.partsCovered);
    }

    public static class AccidentalDamagePolicyDataBuilder {
      private String labourCovered;
      public AccidentalDamagePolicyDataBuilder labourCovered(String labourCovered) { this.labourCovered = labourCovered; return this; }
      private String partsCovered;
      public AccidentalDamagePolicyDataBuilder partsCovered(String partsCovered) { this.partsCovered = partsCovered; return this; }

      public AccidentalDamagePolicyData build() {
        return new AccidentalDamagePolicyData(labourCovered, partsCovered);
      }
    }

    public static AccidentalDamagePolicyData copyFrom(AccidentalDamageQuote.AccidentalDamageQuoteData other) {
      if (other == null) {
        return null;
      }
      return new AccidentalDamagePolicyData(
          other.labourCovered(),
          other.partsCovered()
      );
    }

    public static AccidentalDamagePolicyData copyFrom(AccidentalDamagePolicyData other) {
      if (other == null) {
        return null;
      }
      return new AccidentalDamagePolicyData(
          other.labourCovered(),
          other.partsCovered()
      );
    }
  }

  public static boolean canAccessFields(DataMaskingLevel level, Map<String, Object> fields) {
    return true;
  }
  public static DataMaskingLevel fieldsMaximumLevel(Map<String, Object> fields) {
    DataMaskingLevel maxLevel = DataMaskingLevel.none;
    return maxLevel;
  }
  public static Map<String, Object> anonymizeFields(Map<String, Object> fields) {
    Map<String, Object> result = new HashMap<>();
    for (Map.Entry<String, Object> entry: fields.entrySet()) {
      String keyName = entry.getKey();
      Object fieldValue = entry.getValue();
      result.put(keyName, fieldValue);
    }
    return result;
  }
}