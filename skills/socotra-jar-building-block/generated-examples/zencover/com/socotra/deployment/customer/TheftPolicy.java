/*
  Autogenerated - DO NOT EDIT!
*/
package com.socotra.deployment.customer;

import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonView;
import com.fasterxml.jackson.core.type.*;
import com.socotra.coremodel.*;
import com.socotra.coremodel.constraints.*;
import com.socotra.coremodel.views.Internal;
import com.socotra.deployment.*;
import com.socotra.platform.tools.ULID;

import java.math.*;
import java.time.*;
import java.util.*;
import java.util.function.Consumer;
import java.util.regex.*;
import java.util.stream.Collectors;

public record TheftPolicy(
    @JsonView({Internal.class}) Element element
    ) implements Theft, Validatable<TheftPolicy>, Elemental, CustomerObject {

  public static final String TYPE = "TheftPolicy";

  public String type() { return TYPE; }

  public TheftPolicy maskData(DataMaskingLevel level) {
    return this;
  }

  public TheftPolicy anonymizeData() {
    return this;
  }

  public Instant originalEffectiveTime() {
    return element.originalEffectiveTime().orElse(null);
  }
  @Override
  public Collection<ValidationItem> validate(DeploymentConfig config, ValidatableContext context) {
    Collection<ValidationItem> validationItems = new ArrayList<>();
    ValidationItem.ValidationItemBuilder validationItemBuilder = ValidationItem.builder()
        .elementType(type()).locator(element.locator());
    if (validationItemBuilder.hasErrors()) {
      validationItems.add(validationItemBuilder.build());
    }
    return validationItems;
  }

  @Override
  public TheftPolicy correct(DeploymentConfig config, ValidationErrorResolver resolver) {
    return this;
  }

  public TheftPolicy applyDefaults(DeploymentConfig config) {
    TheftPolicyBuilder builder = toBuilder();
    return builder.build();
  }

  public TheftPolicy applyAvailabilityRemovals(Instant referenceDate) {
    TheftPolicyBuilder builder = toBuilder();
    return builder.build();
  }

  @JsonIgnore
  public ULID locator() {
    return element.locator();
  }

  public Element toElement(DeploymentFactory factory) {
    Map<String, Object> ct = Map.of();
    Collection<Element> subElements = List.of();
    Map<String, Object> data = Map.of();
    return element.toBuilder()
        .type(type())
        .elements(Collections.unmodifiableCollection(subElements))
        .coverageTerms(ct)
        .data(data)
        .category(ElementCategory.coverage)
        .build();
  }

  public static TheftPolicy fromElement(DeploymentFactory factory, Element element) {
    if (element == null) {
      return null;
    }
    return new TheftPolicy(
        element);
  }

  public static TheftPolicy copyFrom(UUID tenantLocator, ULID rootLocator, ULID parentLocator, TheftQuote other) {
    if (other == null) {
      return null;
    }
    TheftPolicyBuilder builder = builder(tenantLocator, rootLocator, parentLocator, other.element().locator(), other.element().originalEffectiveTime().orElse(null));
    return builder
        .build();
  }

  public static TheftPolicy copyFrom(UUID tenantLocator, ULID rootLocator, ULID parentLocator, TheftPolicy other) {
    if (other == null) {
      return null;
    }
    TheftPolicyBuilder builder = builder(tenantLocator, rootLocator, parentLocator, other.element().staticLocator(), other.element().originalEffectiveTime().orElse(null));
    return builder
        .build();
  }

  public static TheftPolicyBuilder builder(UUID tenantLocator, ULID rootLocator, ULID parentLocator, ULID staticLocator, Instant originalEffectiveTime) {
    Element origin = Element.builder()
        .tenantLocator(tenantLocator)
        .type("TheftPolicy")
        .locator(ULID.generate())
        .staticLocator(staticLocator)
        .rootLocator(rootLocator)
        .parentLocator(parentLocator)
        .originalEffectiveTime(originalEffectiveTime)
        .build();
    return new TheftPolicyBuilder().element(origin);
  }

  public TheftPolicyBuilder toBuilder() {
    return new TheftPolicyBuilder()
        .element(this.element);
  }

  public static class TheftPolicyBuilder {
    private Element element;
    public TheftPolicyBuilder element(Element element) { this.element = element; return this; }

    private TheftPolicyBuilder() {}

    public TheftPolicy build() {
      return new TheftPolicy(
          this.element
      );
    }
  }

}