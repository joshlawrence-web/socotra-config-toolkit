/*
  Autogenerated - DO NOT EDIT!
*/
package com.socotra.deployment.customer;

import com.socotra.coremodel.*;
import com.socotra.coremodel.constraints.*;
import com.socotra.deployment.DeploymentConfig;
import com.socotra.platform.tools.ULID;

import java.math.*;
import java.time.*;
import java.util.*;
import java.util.regex.*;
import java.util.stream.Collectors;

public record PersonalAccount(
  ULID locator,
  PersonalAccountData data,
  Optional<String> delinquencyPlanName,
  Optional<String> autoRenewalPlanName,
  Optional<String> excessCreditPlanName,
  Optional<String> shortfallTolerancePlanName,
  Optional<Preferences> preferences,
  BillingLevel billingLevel,
  Optional<String> region,
  Optional<String> invoiceDocument,
  Optional<String> accountNumber,
  Collection<ContactRoles> contacts,
  Optional<String> paymentExecutionRetryPlanName
) implements AccountDefinition, CustomerObject, Validatable<PersonalAccount>, ContactsHolder, NumberingTriggerHolder {

  public static final String TYPE = "PersonalAccount";
  private static final Map<String, ContactSlot> CONTACT_SLOTS = Map.ofEntries(

  );

  public NumberingTrigger numberingTrigger() {
    return NumberingTrigger.validation;
  }

  public PersonalAccount {
    if(data == null) {
      data = PersonalAccountData.builder().build();
    }
    if(contacts == null) {
      contacts = List.of();
    }
    if(invoiceDocument.isEmpty()) {
      invoiceDocument = Optional.empty();
    }
  }

  @Override
  public String type() { return TYPE; }

  public PersonalAccount maskData(DataMaskingLevel level) {
    return this.toBuilder()
        .data(this.data.maskData(level))
        .build();
  }

  public PersonalAccount anonymizeData() {
    return this.toBuilder()
        .data(this.data.anonymizeData())
        .build();
  }

  @Override
  public Map<String, ContactSlot> contactSlots() {
    return CONTACT_SLOTS;
  }

  @Override
  public Collection<ValidationItem> validate(DeploymentConfig config, ValidatableContext context) {
    ValidationItem.ValidationItemBuilder builder = ValidationItem.builder().elementType(TYPE);
    this.data.validate(config, context).forEach(error -> error.errors().forEach(builder::addError));
    this.region.ifPresent(r -> { if (!config.getRegions().contains(r)) { builder.addError("'region' should be one of " + config.getRegions()); }; } );
    builder.addErrors(ContactsHolder.super.validateContacts());
    return builder.hasErrors() ? List.of(builder.build()) : List.of();
  }

  @Override
  public PersonalAccount correct(DeploymentConfig config, ValidationErrorResolver resolver) {
    return toBuilder().data(this.data.correct(config, resolver)).build();
  }

  public static PersonalAccountBuilder builder() {
    return new PersonalAccountBuilder();
  }
  public PersonalAccountBuilder toBuilder() {
    return new PersonalAccountBuilder()
      .locator(this.locator)
      .data(this.data)
      .delinquencyPlanName(this.delinquencyPlanName)
      .autoRenewalPlanName(this.autoRenewalPlanName)
      .excessCreditPlanName(this.excessCreditPlanName)
      .shortfallTolerancePlanName(this.shortfallTolerancePlanName)
      .paymentExecutionRetryPlanName(this.paymentExecutionRetryPlanName)
      .preferences(this.preferences)
      .billingLevel(this.billingLevel)
      .region(this.region)
      .invoiceDocument(this.invoiceDocument)
      .accountNumber(this.accountNumber)
      .contacts(this.contacts());
  }

  public static class PersonalAccountBuilder {
    private ULID locator;
    public PersonalAccountBuilder locator(ULID locator){this.locator = locator; return this;}
    private PersonalAccountData data;
    public PersonalAccountBuilder data(PersonalAccountData data){this.data = data; return this;}
    private Optional<String> delinquencyPlanName;
    private Optional<String> autoRenewalPlanName;
    private Optional<String> excessCreditPlanName;
    private Optional<String> shortfallTolerancePlanName;
    private Optional<String> paymentExecutionRetryPlanName;
    private Optional<Preferences> preferences;
    private BillingLevel billingLevel;
    private Optional<String> invoiceDocument;

    public PersonalAccountBuilder delinquencyPlanName(Optional<String> delinquencyPlanName){this.delinquencyPlanName = delinquencyPlanName; return this;}
    public PersonalAccountBuilder delinquencyPlanName(String delinquencyPlanName){this.delinquencyPlanName = Optional.ofNullable(delinquencyPlanName); return this;}
    public PersonalAccountBuilder autoRenewalPlanName(Optional<String> autoRenewalPlanName){this.autoRenewalPlanName = autoRenewalPlanName; return this;}
    public PersonalAccountBuilder autoRenewalPlanName(String autoRenewalPlanName){this.autoRenewalPlanName = Optional.ofNullable(autoRenewalPlanName); return this;}
    public PersonalAccountBuilder excessCreditPlanName(Optional<String> excessCreditPlanName){this.excessCreditPlanName = excessCreditPlanName; return this;}
    public PersonalAccountBuilder excessCreditPlanName(String excessCreditPlanName){this.excessCreditPlanName = Optional.ofNullable(excessCreditPlanName); return this;}
    public PersonalAccountBuilder shortfallTolerancePlanName(Optional<String> shortfallTolerancePlanName){this.shortfallTolerancePlanName = shortfallTolerancePlanName; return this;}
    public PersonalAccountBuilder shortfallTolerancePlanName(String shortfallTolerancePlanName){this.shortfallTolerancePlanName = Optional.ofNullable(shortfallTolerancePlanName); return this;}
    public PersonalAccountBuilder paymentExecutionRetryPlanName(Optional<String> paymentExecutionRetryPlanName){this.paymentExecutionRetryPlanName = paymentExecutionRetryPlanName; return this;}
    public PersonalAccountBuilder paymentExecutionRetryPlanName(String paymentExecutionRetryPlanName){this.paymentExecutionRetryPlanName = Optional.ofNullable(paymentExecutionRetryPlanName); return this;}
    public PersonalAccountBuilder preferences(Optional<Preferences> preferences){this.preferences = preferences; return this;}
    public PersonalAccountBuilder preferences(Preferences preferences){this.preferences = Optional.ofNullable(preferences); return this;}
    public PersonalAccountBuilder billingLevel(BillingLevel billingLevel){this.billingLevel = billingLevel; return this;}
    private Optional<String> region;
    public PersonalAccountBuilder region(Optional<String> region) { this.region = region; return this; }
    public PersonalAccountBuilder region(String region) { this.region = Optional.ofNullable(region); return this; }
    public PersonalAccountBuilder invoiceDocument(Optional<String> invoiceDocument){this.invoiceDocument = invoiceDocument; return this;}
    public PersonalAccountBuilder invoiceDocument(String invoiceDocument){this.invoiceDocument = Optional.ofNullable(invoiceDocument); return this;}
    private Optional<String> accountNumber;
    public PersonalAccountBuilder accountNumber(Optional<String> accountNumber){this.accountNumber = accountNumber; return this;}
    public PersonalAccountBuilder accountNumber(String accountNumber){this.accountNumber = Optional.ofNullable(accountNumber); return this;}
    private Collection<ContactRoles> contacts;
    public PersonalAccountBuilder contacts(Collection<ContactRoles> contacts) { this.contacts = contacts; return this; }
    public PersonalAccount build() {
      return new PersonalAccount(locator, data, delinquencyPlanName, autoRenewalPlanName, excessCreditPlanName, shortfallTolerancePlanName, preferences, billingLevel, region, invoiceDocument, accountNumber, contacts, paymentExecutionRetryPlanName);
    }
  }

  public record PersonalAccountData(String email, String firstName, String lastName, String primaryPhone) implements CustomerObject, Validatable<PersonalAccountData> {
    public static final String TYPE = "PersonalAccountData";

    public String type() { return TYPE; }

    public PersonalAccountData maskData(DataMaskingLevel level) {
      PersonalAccountDataBuilder builder = toBuilder();
      if (level == DataMaskingLevel.level1) {
      }
      if (level == DataMaskingLevel.level2) {
      }
      if (level == DataMaskingLevel.none) {
      }
      return builder.build();
    }

    public PersonalAccountData anonymizeData() {
      PersonalAccountDataBuilder builder = toBuilder();
      return builder.build();
    }

    @Override
    public Collection<ValidationItem> validate(DeploymentConfig config, ValidatableContext context) {
      ValidationItem.ValidationItemBuilder validationItemBuilder = ValidationItem.builder().elementType(TYPE);
      if (email == null || email.isBlank()) { validationItemBuilder.addError("Non optional property 'personalAccount.email' is missing"); }
      if (email != null && email.length() > 200) { validationItemBuilder.addError("'personalAccount.email' length is more than max length of 200"); }
      if (firstName == null || firstName.isBlank()) { validationItemBuilder.addError("Non optional property 'personalAccount.firstName' is missing"); }
      if (firstName != null && firstName.length() > 200) { validationItemBuilder.addError("'personalAccount.firstName' length is more than max length of 200"); }
      if (lastName == null || lastName.isBlank()) { validationItemBuilder.addError("Non optional property 'personalAccount.lastName' is missing"); }
      if (lastName != null && lastName.length() > 200) { validationItemBuilder.addError("'personalAccount.lastName' length is more than max length of 200"); }
      if (primaryPhone == null || primaryPhone.isBlank()) { validationItemBuilder.addError("Non optional property 'personalAccount.primaryPhone' is missing"); }
      if (primaryPhone != null && primaryPhone.length() > 200) { validationItemBuilder.addError("'personalAccount.primaryPhone' length is more than max length of 200"); }

      return validationItemBuilder.hasErrors() ? List.of(validationItemBuilder.build()) : List.of();
    }

    @Override
    public PersonalAccountData correct(DeploymentConfig config, ValidationErrorResolver resolver) {
      PersonalAccountDataBuilder builder = this.toBuilder();
      Map<String, java.lang.reflect.RecordComponent> fields = Arrays.stream(this.getClass().getRecordComponents()).collect(java.util.stream.Collectors.toMap(java.lang.reflect.RecordComponent::getName, c -> c));
      resolver.correct(this, fields.get("email"), builder::email, new Required<>(email), new MaxLength(200));
      resolver.correct(this, fields.get("firstName"), builder::firstName, new Required<>(firstName), new MaxLength(200));
      resolver.correct(this, fields.get("lastName"), builder::lastName, new Required<>(lastName), new MaxLength(200));
      resolver.correct(this, fields.get("primaryPhone"), builder::primaryPhone, new Required<>(primaryPhone), new MaxLength(200));

      return builder.build();
    }

    @Override
    public PersonalAccountData applyAvailabilityRemovals(Instant referenceDate) {
      PersonalAccountDataBuilder builder = toBuilder();

      return builder.build();
    }

    public static PersonalAccountDataBuilder builder() {
      return new PersonalAccountDataBuilder();
    }
    public PersonalAccountDataBuilder toBuilder() {
      return new PersonalAccountDataBuilder()
      .email(this.email)
      .firstName(this.firstName)
      .lastName(this.lastName)
      .primaryPhone(this.primaryPhone);
    }

    public static class PersonalAccountDataBuilder {
      private String email;
      public PersonalAccountDataBuilder email(String email) { this.email = email; return this; }
      private String firstName;
      public PersonalAccountDataBuilder firstName(String firstName) { this.firstName = firstName; return this; }
      private String lastName;
      public PersonalAccountDataBuilder lastName(String lastName) { this.lastName = lastName; return this; }
      private String primaryPhone;
      public PersonalAccountDataBuilder primaryPhone(String primaryPhone) { this.primaryPhone = primaryPhone; return this; }

      public PersonalAccountData build() {
        return new PersonalAccountData(email, firstName, lastName, primaryPhone);
      }
    }
  }

  public static boolean canAccessFields(DataMaskingLevel level, Map<String, Object> fields) {
    return true;
  }
  public static DataMaskingLevel fieldsMaximumLevel(Map<String, Object> fields) {
    DataMaskingLevel maxLevel = DataMaskingLevel.none;
    return maxLevel;
  }
  public static Map<String, Object> anonymizeFields(Map<String, Object> fields) {
    Map<String, Object> result = new HashMap<>();
    for (Map.Entry<String, Object> entry: fields.entrySet()) {
      String keyName = entry.getKey();
      Object fieldValue = entry.getValue();
      result.put(keyName, fieldValue);
    }
    return result;
  }
}