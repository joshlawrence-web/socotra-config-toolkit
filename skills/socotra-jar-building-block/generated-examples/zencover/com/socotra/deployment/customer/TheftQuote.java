/*
  Autogenerated - DO NOT EDIT!
*/
package com.socotra.deployment.customer;

import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonView;
import com.fasterxml.jackson.core.type.*;
import com.socotra.coremodel.*;
import com.socotra.coremodel.constraints.*;
import com.socotra.coremodel.views.Internal;
import com.socotra.deployment.*;
import com.socotra.platform.tools.ULID;

import java.math.*;
import java.time.*;
import java.util.*;
import java.util.function.Consumer;
import java.util.regex.*;
import java.util.stream.Collectors;

public record TheftQuote(
    @JsonView({Internal.class}) Element element
    ) implements Theft, Validatable<TheftQuote>, Elemental, CustomerObject {

  public static final String TYPE = "TheftQuote";

  public String type() { return TYPE; }

  public TheftQuote maskData(DataMaskingLevel level) {
    return this;
  }

  public TheftQuote anonymizeData() {
    return this;
  }

  @Override
  public Collection<ValidationItem> validate(DeploymentConfig config, ValidatableContext context) {
    Collection<ValidationItem> validationItems = new ArrayList<>();
    ValidationItem.ValidationItemBuilder validationItemBuilder = ValidationItem.builder()
        .elementType(type()).locator(element.locator());
    if (validationItemBuilder.hasErrors()) {
      validationItems.add(validationItemBuilder.build());
    }
    return validationItems;
  }

  @Override
  public TheftQuote correct(DeploymentConfig config, ValidationErrorResolver resolver) {
    return this;
  }

  public TheftQuote applyDefaults(DeploymentConfig config) {
    TheftQuoteBuilder builder = toBuilder();
    return builder.build();
  }

  public TheftQuote applyAvailabilityRemovals(Instant referenceDate) {
    TheftQuoteBuilder builder = toBuilder();
    return builder.build();
  }

  @JsonIgnore
  public ULID locator() {
    return element.locator();
  }

  public Element toElement(DeploymentFactory factory) {
    Map<String, Object> ct = Map.of();
    Collection<Element> subElements = List.of();
    Map<String, Object> data = Map.of();
    return element.toBuilder()
        .type(type())
        .elements(Collections.unmodifiableCollection(subElements))
        .coverageTerms(ct)
        .data(data)
        .category(ElementCategory.coverage)
        .build();
  }

  public static TheftQuote fromElement(DeploymentFactory factory, Element element) {
    if (element == null) {
      return null;
    }
    return new TheftQuote(
        element);
  }

  public static TheftQuoteBuilder builder(UUID tenantLocator, ULID rootLocator, ULID parentLocator, ULID staticLocator, Instant originalEffectiveTime) {
    Element origin = Element.builder()
        .tenantLocator(tenantLocator)
        .type("TheftQuote")
        .locator(ULID.generate())
        .staticLocator(staticLocator)
        .rootLocator(rootLocator)
        .parentLocator(parentLocator)
        .originalEffectiveTime(originalEffectiveTime)
        .build();
    return new TheftQuoteBuilder().element(origin);
  }

  public TheftQuoteBuilder toBuilder() {
    return new TheftQuoteBuilder()
        .element(this.element);
  }

  public static class TheftQuoteBuilder {
    private Element element;
    public TheftQuoteBuilder element(Element element) { this.element = element; return this; }

    private TheftQuoteBuilder() {}

    public TheftQuote build() {
      return new TheftQuote(
          this.element
      );
    }
  }

}