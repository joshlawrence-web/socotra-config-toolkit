/*
Autogenerated - DO NOT EDIT!
*/
package com.socotra.coremodel;

import java.util.*;
import java.util.function.Consumer;
import java.util.stream.*;
import com.socotra.protobuf.*;
import com.socotra.coremodel.protobuf.*;
import com.socotra.deployment.*;
import java.util.UUID;
import java.lang.reflect.Method;
import java.time.*;
import java.math.RoundingMode;
import java.util.function.*;
import com.socotra.platform.tools.ULID;
import com.fasterxml.jackson.core.type.TypeReference;

public record TransactionPricing(
    com.socotra.platform.tools.ULID transactionLocator,
    Collection<Charge> items,
    Collection<TransactionPricing> aggregated
) 
{
  public TransactionPricing {
    items = items == null ? List.of() : List.copyOf(items);
    aggregated = aggregated == null ? List.of() : List.copyOf(aggregated);

    Objects.requireNonNull(transactionLocator, "transactionLocator is required but it is null");

 }

  public com.socotra.coremodel.protobuf.TransactionPricing toProto() {
    com.socotra.coremodel.protobuf.TransactionPricing.Builder builder = com.socotra.coremodel.protobuf.TransactionPricing.newBuilder();
    if (transactionLocator != null) {
      builder.setTransactionLocator(com.socotra.protobuf.ULID.newBuilder().setMsb(transactionLocator.getMostSignificantBits()).setLsb(transactionLocator.getLeastSignificantBits()).build());
    }
    builder.addAllItems(this.items.stream().map(f -> f.toProto()).toList());
    builder.addAllAggregated(this.aggregated.stream().map(f -> f.toProto()).toList());
    return builder.build();
  }

  public TransactionPricing toTransport() {
    return toBuilder()
        .items(List.of())
        .aggregated(List.of())
        .build();
  }

  public static TransactionPricing fromProto(com.socotra.coremodel.protobuf.TransactionPricing proto) {
    return new TransactionPricing(
         new com.socotra.platform.tools.ULID(proto.getTransactionLocator().getMsb(), proto.getTransactionLocator().getLsb()),
         proto.getItemsList().stream().map(f -> Charge.fromProto(f)).toList(),
         proto.getAggregatedList().stream().map(f -> TransactionPricing.fromProto(f)).toList()
    );
  }

  public static TransactionPricingBuilder builder() {
    return new TransactionPricingBuilder();
  }


  public TransactionPricingBuilder toBuilder() {
    return new TransactionPricingBuilder()
       .transactionLocator(this.transactionLocator)
       .items(this.items)
       .aggregated(this.aggregated);
  }




  public static class TransactionPricingBuilder {
    private com.socotra.platform.tools.ULID transactionLocator;
    public TransactionPricingBuilder transactionLocator(com.socotra.platform.tools.ULID transactionLocator) {
      this.transactionLocator = transactionLocator;
      return this;
    }
    private Collection<Charge> items;
    public TransactionPricingBuilder items(Collection<Charge> items) {
      this.items = items;
      return this;
    }
    private Collection<TransactionPricing> aggregated;
    public TransactionPricingBuilder aggregated(Collection<TransactionPricing> aggregated) {
      this.aggregated = aggregated;
      return this;
    }
    public TransactionPricingBuilder() {
      items = new ArrayList<>();
      aggregated = new ArrayList<>();
    }
    public TransactionPricing build() {
      return new TransactionPricing(
          transactionLocator,
          items,
          aggregated
      );
    }

    private <T> Collection<T> copyIfImmutable(Collection<T> c, Consumer<Collection<T>> consumer) {
      if(c == null) {
        c = new ArrayList<>();
      }
      try {
        consumer.accept(c);
      } catch(UnsupportedOperationException e) {
        c = new ArrayList<>(c);
        consumer.accept(c);
      }
      return c;
    }

    public TransactionPricingBuilder addItem(Charge field) {
      this.items = copyIfImmutable(this.items, c -> c.add(field));
      return this;
    }
    public TransactionPricingBuilder addItems(Collection<Charge> fields) {
      this.items = copyIfImmutable(this.items, c -> c.addAll(fields));
      return this;
    }
    public boolean hasItems() {
      return !this.items.isEmpty();
    }
    public TransactionPricingBuilder addAggregated(TransactionPricing field) {
      this.aggregated = copyIfImmutable(this.aggregated, c -> c.add(field));
      return this;
    }
    public TransactionPricingBuilder addAggregated(Collection<TransactionPricing> fields) {
      this.aggregated = copyIfImmutable(this.aggregated, c -> c.addAll(fields));
      return this;
    }
    public boolean hasAggregated() {
      return !this.aggregated.isEmpty();
    }


  }
}
