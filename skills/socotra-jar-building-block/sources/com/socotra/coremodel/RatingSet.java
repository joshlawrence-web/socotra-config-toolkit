/*
Autogenerated - DO NOT EDIT!
*/
package com.socotra.coremodel;

import java.util.*;
import java.util.function.Consumer;
import java.util.stream.*;
import com.socotra.protobuf.*;
import com.socotra.coremodel.protobuf.*;
import com.socotra.deployment.*;
import java.util.UUID;
import java.lang.reflect.Method;
import java.time.*;
import java.math.RoundingMode;
import java.util.function.*;
import com.socotra.platform.tools.ULID;
import com.fasterxml.jackson.core.type.TypeReference;

public record RatingSet(
    java.lang.Boolean ok,
    Collection<RatingItem> ratingItems
) 
{
  public RatingSet {
    ratingItems = ratingItems == null ? List.of() : List.copyOf(ratingItems);
Set<String> validationSet = new HashSet<>();
for (RatingItem item : ratingItems) {
  if (validationSet.contains(item.elementLocator() + item.chargeType().name())) {
    throw new IllegalArgumentException(
      "Element["
        + item.elementLocator()
        + "] has already a rating item with chargeType: "
        + item.chargeType().name());
  }
  validationSet.add(item.elementLocator() + item.chargeType().name());
}
    Objects.requireNonNull(ok, "ok is required but it is null");

 }

  public com.socotra.coremodel.protobuf.RatingSet toProto() {
    com.socotra.coremodel.protobuf.RatingSet.Builder builder = com.socotra.coremodel.protobuf.RatingSet.newBuilder();
    if (ok != null) {
      builder.setOk(this.ok);
    }
    builder.addAllRatingItems(this.ratingItems.stream().map(f -> f.toProto()).toList());
    return builder.build();
  }

  public RatingSet toTransport() {
    return toBuilder()
        .ratingItems(List.of())
        .build();
  }

  public static RatingSet fromProto(com.socotra.coremodel.protobuf.RatingSet proto) {
    return new RatingSet(
         proto.getOk(),
         proto.getRatingItemsList().stream().map(f -> RatingItem.fromProto(f)).toList()
    );
  }

  public static RatingSetBuilder builder() {
    return new RatingSetBuilder();
  }


  public RatingSetBuilder toBuilder() {
    return new RatingSetBuilder()
       .ok(this.ok)
       .ratingItems(this.ratingItems);
  }




  public static class RatingSetBuilder {
    private java.lang.Boolean ok;
    public RatingSetBuilder ok(java.lang.Boolean ok) {
      this.ok = ok;
      return this;
    }
    private Collection<RatingItem> ratingItems;
    public RatingSetBuilder ratingItems(Collection<RatingItem> ratingItems) {
      this.ratingItems = ratingItems;
      return this;
    }
    public RatingSetBuilder() {
      ratingItems = new ArrayList<>();
    }
    public RatingSet build() {
      return new RatingSet(
          ok,
          ratingItems
      );
    }

    private <T> Collection<T> copyIfImmutable(Collection<T> c, Consumer<Collection<T>> consumer) {
      if(c == null) {
        c = new ArrayList<>();
      }
      try {
        consumer.accept(c);
      } catch(UnsupportedOperationException e) {
        c = new ArrayList<>(c);
        consumer.accept(c);
      }
      return c;
    }

    public RatingSetBuilder addRatingItem(RatingItem field) {
      this.ratingItems = copyIfImmutable(this.ratingItems, c -> c.add(field));
      return this;
    }
    public RatingSetBuilder addRatingItems(Collection<RatingItem> fields) {
      this.ratingItems = copyIfImmutable(this.ratingItems, c -> c.addAll(fields));
      return this;
    }
    public boolean hasRatingItems() {
      return !this.ratingItems.isEmpty();
    }

    @Deprecated
    public RatingSetBuilder addRatingItems(RatingItem... items) {
      this.ratingItems = this.copyIfImmutable(this.ratingItems, (c) -> {
        c.addAll(Arrays.asList(items));
      });
      return this;
    }
  }
}
